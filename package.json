{
  "name": "peak-district-downhill",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "build:itch": "vite build --mode itch --outDir dist-itch",
    "package:itch": "npm run build:itch && node tools/itch/package.js",
    "preview": "vite preview --host",
    "test": "vitest run",
    "test:e2e": "playwright test",
    "terrain:build": "node tools/terrain/buildTerrain.js && node tools/terrain/fetchRoute.js && node tools/terrain/fetchLandcover.js && node tools/terrain/fetchPaths.js",
    "terrain:trees": "node tools/terrain/buildTrees.js",
    "terrain:placeholder": "node tools/terrain/generatePlaceholder.js"
  },
  "dependencies": {
    "three": "0.185.1",
    "cannon-es": "0.20.0"
  },
  "devDependencies": {
    "vite": "8.1.3",
    "geotiff": "^3.0.5",
    "proj4": "^2.15.0",
    "vitest": "^4.1.9",
    "jsdom": "^29.1.1",
    "@playwright/test": "^1.61.1"
  }
}
